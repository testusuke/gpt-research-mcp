[project]
name = "gpt-research-mcp"
version = "0.1.0"
description = "GPT-5.1 Web Search を使用したリサーチ MCP サーバー"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0.0",
    "langfuse>=3.10.3",
    "openai>=1.0.0",
]
